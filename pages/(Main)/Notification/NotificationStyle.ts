import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  notificationContainer: {
    paddingTop: 50,
  },
  drawerContent: {
    width: "100%",
    height: "100%",
    position: "absolute",
  },
});
